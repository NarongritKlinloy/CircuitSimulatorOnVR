using UnityEngine;

public interface INotGate
{
    // ตัวอย่าง: ถ้าต้องการให้ผู้ใช้ปรับตั้งค่าเกณฑ์แรงดัน (threshold) ก็สามารถใส่ property เพิ่มได้
    // double ThresholdVoltage { get; }
    //
    // สำหรับตัวอย่างง่าย ๆ อาจจะไม่ต้องมี property ใด ๆ ก็ได้
}
